Work to deploy ctf-performance

Per-user isolation
- Replace shared-port challenges with per-user stacks (compose project name ctf-<user>, unique subdomain or port range each)
- Apply CPU/memory limits to all stacks including the databases, not just some
- Optional: CTFd-whale plugin if you want on-demand spawn/reap instead of static allocation

Per-user reset
- Replace global reset-environments.sh with per-user "down -v && up -d" on that user's project
- Wrap in a script or one-button admin action

Flag trust (only if the event is competitive)
- Stop the verifier returning a flag the participant can read via docker exec
- Inject flag from deploy-time secret or issue per-user random flags, or have verifier report solve to CTFd API
- Skip entirely if it's an honor-system learning day

Hosting
- One EC2 instance, t3.xlarge or t3.2xlarge, plain Docker + compose
- Security group locked to Cloudflare IP ranges plus your SSH
- Tear down after the event

Domain + DNS
- Register one domain (Cloudflare Registrar or Route53)
- Wildcard DNS record pointing at the EC2 elastic IP, proxied through Cloudflare

TLS + reverse proxy
- Caddy on the box for automatic Let's Encrypt certs and subdomain to container routing
- Cloudflare proxied in front for edge TLS

Auth
- Cloudflare Access (Zero Trust) in front of CTFd for SSO or email-OTP login, terminates TLS before requests reach CTFd

Loose ends
- Set a real SECRET_KEY for CTFd (currently change-me-in-production)
- infrastructure/k6 and infrastructure/monitoring are empty, fill or drop them
- Import challenges into CTFd via import-challenges.sh after CTFd is up